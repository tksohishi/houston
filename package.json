{
  "name": "houston",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts --verbose",
    "setup": "bun run scripts/setup.ts",
    "reset": "bun run scripts/setup.ts --reset-only",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "discord.js": "^14.16.3"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "bun-types": "^1.3.10",
    "typescript": "^5.8.2"
  }
}
